// Plain stylesheet imports (e.g. import './globals.css') are handled by Next.js.
// Newer TypeScript versions check side-effect imports, and Next's own types only
// cover *.module.css, so declare plain .css files here.
declare module '*.css';
